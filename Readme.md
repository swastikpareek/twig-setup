# Contains code for setting up simple PHP server to use Twig


## INSTALLING INSTRUCTION

 * Run install.sh

```shell
$ sh install.sh
```


 * Add twig extension in php.ini ( For UNIX system )

```php
  $extension=twig.so
```
